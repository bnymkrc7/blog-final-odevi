
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title><%= post.title %> - Bünyamin Kıraç</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-light d-flex flex-column min-vh-100">

    <header class="py-3 bg-white border-bottom shadow-sm">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/" class="text-decoration-none">
                <h4 class="fw-bold text-dark mb-0" style="font-family: 'Georgia', serif;">BÜNYAMİN KIRAÇ</h4>
            </a>
            <nav>
                <a href="/" class="btn btn-outline-dark rounded-pill me-2 btn-sm">Ana Sayfa</a>
                <a href="/about" class="btn btn-outline-dark rounded-pill me-2 btn-sm">Hakkımda</a>
                <a href="/contact" class="btn btn-outline-dark rounded-pill btn-sm">İletişim</a>
            </nav>
        </div>
    </header>

    <div class="container mt-5 flex-grow-1">
        <div class="row justify-content-center">
            <div class="col-md-9">
                
                <div class="card shadow border-0 rounded-3 overflow-hidden">
                    <% if(post.image_url) { %>
                        <img src="<%= post.image_url %>" class="card-img-top" alt="Resim" style="max-height: 400px; object-fit: cover;">
                    <% } %>
                    
                    <div class="card-body p-5">
                        <h1 class="card-title fw-bold mb-3 display-5"><%= post.title %></h1>
                        <p class="text-muted mb-4 border-bottom pb-3">
                            <i class="far fa-calendar-alt me-1"></i> 
                            <%= new Date(post.created_at).toLocaleDateString() %>
                        </p>
                        
                        <div class="card-text fs-5 lh-lg text-dark">
                            <%- post.content.replace(/\n/g, '<br>') %>
                        </div>
                    </div>
                </div>

                <div class="mt-5 mb-5">
                    <h3 class="fw-bold mb-4">💬 Yorumlar (<%= comments ? comments.length : 0 %>)</h3>

                    <% if(typeof loggedin !== 'undefined' && loggedin) { %>
                        <div class="card shadow-sm mb-4 border-0 bg-white">
                            <div class="card-body p-4">
                                <h6 class="mb-3">Sayın <strong><%= user %></strong>, düşüncelerinizi paylaşın:</h6>
                                <form action="/post/<%= post.id %>/comment" method="POST">
                                    <textarea name="comment" class="form-control mb-3 bg-light" rows="3" placeholder="Buraya bir şeyler yaz..." required></textarea>
                                    <div class="text-end">
                                        <button type="submit" class="btn btn-dark px-4 rounded-pill">Yorumu Gönder</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    <% } else { %>
                        <div class="alert alert-secondary border-0 shadow-sm">
                            Yorum yapabilmek için lütfen <a href="/login" class="fw-bold text-dark text-decoration-underline">Giriş Yapın</a>.
                        </div>
                    <% } %>

                    <% if(typeof comments !== 'undefined' && comments.length > 0) { %>
                        <% comments.forEach(function(comment) { %>
                            <div class="d-flex mb-4">
                                <div class="flex-shrink-0">
                                    <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 50px; height: 50px; font-size: 20px;">
                                        <%= comment.username.charAt(0).toUpperCase() %>
                                    </div>
                                </div>
                                <div class="flex-grow-1 ms-3 bg-white p-3 rounded shadow-sm border">
                                    <h6 class="fw-bold mb-1"><%= comment.username %></h6>
                                    <small class="text-muted d-block mb-2"><%= new Date(comment.created_at).toLocaleDateString() %></small>
                                    <p class="mb-0 text-secondary"><%= comment.comment %></p>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p class="text-muted fst-italic text-center py-3">Henüz hiç yorum yapılmamış. İlk yorumu sen yap! 🚀</p>
                    <% } %>

                </div>
                </div>
        </div>
    </div>

    <footer class="bg-white text-dark text-center py-5 mt-5 border-top">
        <div class="container">
            <h5 class="fw-bold mb-4">BÜNYAMİN KIRAÇ</h5>
            <div class="mb-4 d-flex justify-content-center gap-4">
                <a href="#" class="text-decoration-none text-dark"><i class="fab fa-instagram fa-2x"></i></a>
                <a href="#" class="text-decoration-none text-dark"><i class="fab fa-twitter fa-2x"></i></a>
                <a href="#" class="text-decoration-none text-dark"><i class="fab fa-linkedin fa-2x"></i></a>
                <a href="#" class="text-decoration-none text-dark"><i class="fab fa-github fa-2x"></i></a>
            </div>
            <small class="text-muted">&copy; 2026 Tüm Hakları Saklıdır.</small>
        </div>
    </footer>

</body>
</html>